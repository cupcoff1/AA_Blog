server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aa_blog?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: qbr542463679
    driver-class-name: com.mysql.cj.jdbc.Driver

upload:
  base-dir: ./uploads/blog/
  max-size: 2097152  # 2MB

jwt:
  secret: aa_blog_secret_key_2026_abcdefghijklmnop
  ttl: 604800  # 7天 (秒)

github:
  client-id: ${GITHUB_CLIENT_ID:}
  client-secret: ${GITHUB_CLIENT_SECRET:}
  redirect-uri: ${GITHUB_REDIRECT_URI:http://localhost:8080/api/auth/github/callback}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
