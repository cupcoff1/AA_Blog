# 复制为 .env，填入真实值
DB_PASSWORD=你的数据库密码
JWT_SECRET=至少32字节的随机密钥（可用 openssl rand -base64 48 生成）
ADMIN_INIT_PASSWORD=管理员初始密码（仅在首次启动、管理员表为空时使用）
GITHUB_CLIENT_ID=你的_Client_ID
GITHUB_CLIENT_SECRET=你的_Client_Secret
GITHUB_REDIRECT_URI=http://localhost:5173/auth/callback
