# 个人博客 - 前端设计

## 1. 技术栈

| 层 | 技术 |
|---|---|
| 框架 | Vue 3（Composition API，`<script setup lang="ts">`） |
| 语言 | TypeScript |
| 构建 | Vite 8 |
| 路由 | Vue Router 4 |
| HTTP | Axios（泛型封装，响应拦截器自动解包 Result.data） |
| Markdown | marked + marked-highlight + highlight.js + DOMPurify（XSS 消毒） |
| 评论 | Utterances（GitHub Issues 评论 iframe） |
| 图标 | Lucide Vue |
| 主题 | CSS 变量 + localStorage 持久化 |

## 2. 视觉风格

仿 tania.dev 极简设计。

**布局** — 左侧侧边栏 + 右侧内容区，桌面端 900px+ 显示侧边栏，移动端隐藏。

**配色**
- 亮色：米白底 `#fdf9ee` + 深灰文字 + `#b12d6c` 粉色链接
- 暗色：深灰底 `#1c1c20` + 浅灰文字 + `#c892e7` 紫色链接

## 3. 路由表

### 前台

| 路由 | 组件 | 说明 |
|---|---|---|
| `/` | HomePage.vue | 首页（引语轮播 + Hero 图 + 最新内容） |
| `/blog` | BlogListPage.vue | 博客列表（搜索） |
| `/blog/:slug` | BlogDetailPage.vue | 博客详情（Markdown + 目录 + Utterances 评论） |
| `/notes` | NotesPage.vue | 笔记列表（搜索 + 全文渲染） |
| `/projects` | ProjectsPage.vue | 项目展示 |
| `/about` | AboutPage.vue | 便签墙（管理员） |
| `/guest` | GuestWall.vue | Leave a Note（游客便签墙） |

### 后台

| 路由 | 组件 | 说明 |
|---|---|---|
| `/admin/login` | admin/LoginPage.vue | 管理员登录 |
| `/admin` | 重定向 → `/admin/home` | |
| `/admin/home` | admin/HomeAdminPage.vue | 首页管理（引语 + Hero 图 + 改密码） |
| `/blog/new` | admin/BlogEditPage.vue | 新建文章 |
| `/blog/:id/edit` | admin/BlogEditPage.vue | 编辑文章 |
| `/notes/new` | admin/NotesEditPage.vue | 新建笔记 |
| `/notes/:id/edit` | admin/NotesEditPage.vue | 编辑笔记 |
| `/projects/new` | admin/ProjectsEditPage.vue | 新建项目 |
| `/projects/:id/edit` | admin/ProjectsEditPage.vue | 编辑项目 |

### Auth

| 路由 | 组件 | 说明 |
|---|---|---|
| `/auth/callback` | auth/CallbackPage.vue | GitHub OAuth 回调 |

### 404

`/:pathMatch(.*)*` → NotFoundPage.vue

## 4. 组件

### 布局
- `App.vue` — 根布局
- `components/Sidebar.vue` — 侧边栏导航（Logo + 导航链接 + 主题切换 + 退出）

### 共享组件
- `components/LogoIcon.vue` — SVG Logo 图标
- `components/TagEditor.vue` — 标签编辑组件（v-model）

### Auth 模块
- `router/auth.ts` — 全局认证状态（`isAdmin` ref + `refreshAuth()`），路由守卫和 Sidebar 共享

### 前台
- `views/HomePage.vue` — Hero 区 + 最新 Blog/Notes/Projects
- `views/BlogListPage.vue` — 搜索 + 文章列表 + 管理按钮
- `views/BlogDetailPage.vue` — Markdown + 目录 + Utterances 评论
- `views/NotesPage.vue` — 搜索 + 笔记列表 + 管理按钮
- `views/ProjectsPage.vue` — 项目卡片 + 管理按钮
- `views/AboutPage.vue` — 管理员便签墙（增删）
- `views/GuestWall.vue` — Leave a Note（GitHub 登录 → 分类便签墙 → 删自己的）
- `views/auth/CallbackPage.vue` — GitHub OAuth 回调处理

### 后台
- `views/admin/LoginPage.vue` — 登录
- `views/admin/HomeAdminPage.vue` — 引语 CRUD + Hero 图上传 + 改密码
- `views/admin/BlogEditPage.vue` — 文章编辑（Markdown + 图片上传 + 标签）
- `views/admin/NotesEditPage.vue` — 笔记编辑
- `views/admin/ProjectsEditPage.vue` — 项目编辑

## 5. 认证流程

**管理员** — 用户名密码登录 → 后端设 httpOnly Cookie `admin_token` → 调 `/api/admin/status` 确认 → 侧边栏出现管理按钮。前端不接触 token。

**游客** — GitHub 登录 → OAuth 回调 → 后端设 httpOnly Cookie `commenter_token` → 调 `/api/auth/status` 获取用户信息 → Leave a Note 页可发便签。

**路由守卫** — `router.beforeEach` 检查 `/admin/*` 及编辑页路由，未认证重定向 `/admin/login`。认证状态通过 `/api/admin/status` 获取并缓存。

## 6. API 封装

```typescript
const api = {
  get<T>(url, config?): Promise<T>,      // 自动解包 Result.data
  post<T>(url, data?, config?): Promise<T>,
  put<T>(url, data?, config?): Promise<T>,
  delete<T>(url, config?): Promise<T>,
}
```

`withCredentials: true` 自动携带 httpOnly Cookie。

**请求** — 无需手动附加 token，Cookie 自动发送。

**响应拦截** — 自动解包 `Result.data`：
- `code === 200` → 返回 `data`
- `code !== 200` → 抛出 `Error(result.message)`
- HTTP 401 → 路径含 `/admin` 则跳转 `/admin/login`

## 7. 状态管理

| 状态 | 存储方式 | 说明 |
|---|---|---|
| admin_token | httpOnly Cookie | JS 不可读写，防 XSS 窃取 |
| commenter_token | httpOnly Cookie | 同上 |
| commenter (name, avatar) | localStorage (JSON) | 非敏感信息，前端直接读 |
| 主题 (dark/light) | localStorage + CSS 变量 | 页面加载时同步 `body.dark` |
| isAdmin | `router/auth.ts` 共享 ref | 首次访问 admin 路由时调 `/admin/status`，结果缓存 |

**isAdmin 获取流程：**

```
用户访问 /admin/** 或 /blog/new 等编辑页
  → router.beforeEach 检查 needsAuth(to.path)
    → isAdmin 为 null（未检查）→ 调 refreshAuth() → GET /admin/status
    → isAdmin = false → 重定向 /admin/login
    → isAdmin = true → 放行
  → Sidebar.onMounted → 若 isAdmin 仍为 null 则 refreshAuth()
  → LoginPage 登录成功 → refreshAuth() 设 isAdmin = true → router.push('/')
  → Sidebar 退出 → POST /admin/logout → refreshAuth() 设 isAdmin = false
```
